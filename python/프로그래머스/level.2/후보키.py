from itertools import combinations, product
relation = [["100", "ryan", "music", "2"], ["200", "apeach", "math", "2"], ["300", "tube", "computer", "3"],
            ["400", "con", "computer", "4"], ["500", "muzi", "music", "3"], ["600", "apeach", "music", "2"]]

# 딕셔너리?
# column = []
# # tmp = []
# tmp = [["" for _ in range(len(relation))] for _ in range(len(relation[0]))]
# for i in range(len(relation[0])):  # 열 별로 리스트 만들기
#     for j in range(len(relation)):
#         tmp[i][j] == relation[j][i]
# print(tmp)

row = len(relation)
col = len(relation[0])

comb = []
for i in comb
print(comb)
